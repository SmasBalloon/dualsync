{
  "name": "hono",
  "scripts": {
    "dev": "npm run --hot src/index.ts",
    "db:generate": "prisma generate",
    "db:push": "prisma db push",
    "db:migrate": "prisma migrate dev",
    "db:studio": "prisma studio"
  },
  "dependencies": {
    "@prisma/client": "^7.2.0",
    "hono": "^4.11.3"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "prisma": "^7.2.0"
  }
}
