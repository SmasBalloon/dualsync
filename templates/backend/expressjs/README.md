# expressjs

To install dependencies:

```bash
npm install
```

To run:

```bash
npm run 
```

This project was created using `npm init` in bun v1.3.5. [Bun](https://bun.com) is a fast all-in-one JavaScript runtime.
